import java.math.*;
import java.util.stream.*;
import java.util.*;
import java.io.*;
import java.util.function.Predicate;
import java.lang.*;
public class GFG
{
	public static void main(String[] args) {
			BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
			List<Integer> ilist = new ArrayList<>();
			ilist.add(2);
			ilist.add(0);
			ilist.add(55);
			ilist.add(1);
			ilist.add(33);

			// indexOfSublist, lastIndexOfSubList, min, max, frequency, replaceAll
			// Set<Integer> hs = new HashSet<>();
			// hs.add(5);
			int[] iarr = new int[]{1,2,3,4,5,6,7};
			Set<Integer> sett = new HashSet<>(Arrays.asList(Arrays.stream(iarr).boxed().toArray(Integer[]::new)));
			// Set<Integer> sett2 = new HashSet<>();
			Map<Integer, Integer> mapp =  new HashMap<>();
			mapp.put(1, 11);
			mapp.put(2, 22);
			mapp.put(3, 33);
			mapp.put(4, 44);
			mapp.put(5, 55);
			mapp.put(6, 66);
			mapp.put(7, 77);
			mapp.put(8, 88);
			// ilist.
			// Stack<Integer> st = new Stack<>();
			// Queue<Integer> q = new Queue<>();
			// PriorityQueue<Integer> pq = new PriorityQueue();
			// Deque<Integer> dq = new LinkedList<>();

			

			System.out.println(


				);
		// Integer.parseInt("127", 2);

		// String.
		// int i = 123456;
		// char[] carr = new char[]{'a','b','c','d','e'};
		// System.out.println(String.valueOf(carr,0,2));

		//Long
		//Double.
		//Collections.

	}
}